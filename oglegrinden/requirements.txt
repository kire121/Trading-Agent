# Oglegrinden strategy module -- scoped requirements.
# The rest of this repository (the DQN trading agent) has its own,
# separate, undeclared dependency set (pygame, tensorflow, ta, gym, ...);
# this file covers only the oglegrinden/ package.
numpy>=2.0
pandas>=2.2
scipy>=1.13
ripser>=0.6
persim>=0.3
requests>=2.31
statsmodels>=0.14
matplotlib>=3.8
pytest>=8.0
